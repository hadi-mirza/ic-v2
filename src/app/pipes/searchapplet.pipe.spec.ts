import { SearchappletPipe } from './searchapplet.pipe';

describe('SearchappletPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchappletPipe();
    expect(pipe).toBeTruthy();
  });
});
